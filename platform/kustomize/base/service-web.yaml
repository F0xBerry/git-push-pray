apiVersion: v1
kind: Service
metadata:
  name: scout-web
  labels:
    app.kubernetes.io/name: scout-web
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: scout-web
  ports:
    - name: http
      port: 80
      targetPort: http
