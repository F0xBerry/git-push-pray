apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Add manifests when services exist, e.g.:
# resources:
#   - deployment-api.yaml
#   - deployment-worker.yaml
#   - service-api.yaml

resources: []

commonLabels:
  app.kubernetes.io/part-of: scout
